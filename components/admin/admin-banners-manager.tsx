"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import type { Banner } from "@/lib/types";
import { Alert } from "@/components/ui/alert";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
const empty = { title: "", description: "", image_url: "https://images.unsplash.com/photo-1516321318423-f06f85e504b3?auto=format&fit=crop&w=1600&q=80", button_text: "استعرض الخدمات", button_link: "/services", sort_order: "999", is_enabled: true };
export function AdminBannersManager({ banners }: { banners: Banner[] }) { const router=useRouter(); const [editingId,setEditingId]=useState<string|null>(null); const [form,setForm]=useState(empty); const [message,setMessage]=useState<string|null>(null); const [loading,setLoading]=useState(false); const reset=()=>{setEditingId(null);setForm(empty)}; const save=async()=>{setLoading(true); const res=await fetch(editingId?`/api/admin/banners/${editingId}`:'/api/admin/banners',{method:editingId?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,sort_order:Number(form.sort_order||999)})}); const data=await res.json(); setLoading(false); setMessage(data.message || (res.ok?'تم الحفظ.':'تعذر الحفظ.')); if(res.ok){reset();router.refresh();}}; const del=async(id:string)=>{if(!window.confirm('حذف البانر؟')) return; const res=await fetch(`/api/admin/banners/${id}`,{method:'DELETE'}); const data=await res.json(); setMessage(data.message||''); if(res.ok) router.refresh();}; return <div className="grid gap-8 lg:grid-cols-[380px_1fr]"><Card className="h-fit"><CardHeader><CardTitle>{editingId?'تعديل البانر':'إضافة بانر جديد'}</CardTitle></CardHeader><CardContent className="space-y-4">{message?<Alert>{message}</Alert>:null}<Input placeholder="العنوان" value={form.title} onChange={(e)=>setForm((c)=>({...c,title:e.target.value}))} /><Textarea placeholder="الوصف" value={form.description} onChange={(e)=>setForm((c)=>({...c,description:e.target.value}))} /><Input placeholder="رابط الصورة" value={form.image_url} onChange={(e)=>setForm((c)=>({...c,image_url:e.target.value}))} /><Input placeholder="نص الزر" value={form.button_text} onChange={(e)=>setForm((c)=>({...c,button_text:e.target.value}))} /><Input placeholder="رابط الزر" value={form.button_link} onChange={(e)=>setForm((c)=>({...c,button_link:e.target.value}))} /><div className="grid gap-3 md:grid-cols-2"><Input placeholder="الترتيب" type="number" value={form.sort_order} onChange={(e)=>setForm((c)=>({...c,sort_order:e.target.value}))} /><label className="flex h-11 items-center gap-3 rounded-2xl border border-border px-4 text-sm text-foreground"><input type="checkbox" checked={form.is_enabled} onChange={(e)=>setForm((c)=>({...c,is_enabled:e.target.checked}))} />مفعل</label></div><div className="flex gap-3"><Button type="button" onClick={save} disabled={loading}>{loading?'جارٍ الحفظ...':editingId?'حفظ التعديلات':'إضافة البانر'}</Button>{editingId?<Button type="button" variant="outline" onClick={reset}>إلغاء</Button>:null}</div></CardContent></Card><div className="space-y-5"><h1 className="text-3xl font-black text-foreground">إدارة البانرات</h1>{banners.map((banner)=><Card key={banner.id}><CardContent className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><h2 className="text-lg font-bold text-foreground">{banner.title}</h2><p className="mt-2 text-sm text-muted-foreground">{banner.description}</p><p className="mt-3 text-xs text-primary">{banner.button_text} → {banner.button_link}</p></div><div className="flex gap-3"><Button type="button" variant="secondary" onClick={()=>{setEditingId(banner.id);setForm({title:banner.title,description:banner.description,image_url:banner.image_url,button_text:banner.button_text,button_link:banner.button_link,sort_order:String(banner.sort_order||999),is_enabled:banner.is_enabled});window.scrollTo({top:0,behavior:'smooth'});}}>تعديل</Button><Button type="button" variant="danger" onClick={()=>del(banner.id)}>حذف</Button></div></CardContent></Card>)}</div></div>; }
