import { NextResponse } from "next/server";
import { assertAdminAccess } from "@/lib/admin";
import { normalizeServiceFields } from "@/lib/order";
export async function POST(request: Request) { const auth = await assertAdminAccess(); if (!auth.ok) return NextResponse.json({ message: auth.message }, { status: 403 }); try { const body = await request.json(); const fields = normalizeServiceFields(body.fields); if (!body.title || !body.slug || !body.category_id || !body.description) return NextResponse.json({ message: "بيانات الخدمة الأساسية مطلوبة." }, { status: 400 }); const payload = { category_id: body.category_id, slug: body.slug, title: body.title, image_url: body.image_url, description: body.description, price_usd: Number(body.price_usd || 0), starting_price: Number(body.price_usd || 0) > 0 ? Number(body.price_usd || 0) : null, delivery_time_text: body.delivery_time_text || null, is_active: Boolean(body.is_active), is_featured: Boolean(body.is_featured), is_swap_service: Boolean(body.is_swap_service), meta_title: body.meta_title || null, meta_description: body.meta_description || null, sort_order: Number(body.sort_order || 999) }; const { data: service, error } = await auth.supabase.from("services").insert(payload).select("id").single(); if (error || !service) return NextResponse.json({ message: error?.message || "فشل إضافة الخدمة." }, { status: 500 }); if (fields.length) { const { error: fieldsError } = await auth.supabase.from("service_fields").insert(fields.map((field, index) => ({ service_id: service.id, field_key: field.field_key, field_label: field.field_label, field_type: field.field_type, placeholder: field.placeholder || null, is_required: field.is_required, options: field.options || [], sort_order: Number(field.sort_order || index + 1) }))); if (fieldsError) { await auth.supabase.from("services").delete().eq("id", service.id); return NextResponse.json({ message: fieldsError.message }, { status: 500 }); } } await auth.supabase.from("admin_logs").insert({ admin_user_id: auth.userId, action: "create_service", target_table: "services", target_id: service.id, details: { title: body.title, slug: body.slug } }); return NextResponse.json({ message: "تمت إضافة الخدمة بنجاح." }); } catch (error: any) { return NextResponse.json({ message: error?.message || "حدث خطأ غير متوقع." }, { status: 500 }); } }
