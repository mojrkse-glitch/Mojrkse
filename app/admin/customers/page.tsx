import { Card, CardContent } from "@/components/ui/card";
import { getAdminCustomers } from "@/lib/data-access";
import { formatDate } from "@/lib/utils";
export default async function AdminCustomersPage() { const customers = await getAdminCustomers(); return <div className="space-y-5"><div><h1 className="text-3xl font-black text-foreground">إدارة العملاء</h1><p className="mt-3 text-muted-foreground">عرض سريع لبيانات الحسابات وعدد الطلبات لكل عميل.</p></div>{customers.map((customer)=><Card key={customer.id}><CardContent className="grid gap-4 md:grid-cols-5"><div><p className="text-xs text-muted-foreground">الاسم</p><p className="mt-2 font-semibold text-foreground">{customer.full_name || 'بدون اسم'}</p></div><div><p className="text-xs text-muted-foreground">البريد الإلكتروني</p><p className="mt-2 font-semibold text-foreground">{customer.email || '—'}</p></div><div><p className="text-xs text-muted-foreground">الهاتف</p><p className="mt-2 font-semibold text-foreground">{customer.phone || '—'}</p></div><div><p className="text-xs text-muted-foreground">عدد الطلبات</p><p className="mt-2 font-semibold text-primary">{customer.orders_count || 0}</p></div><div><p className="text-xs text-muted-foreground">تاريخ الانضمام</p><p className="mt-2 font-semibold text-foreground">{formatDate(customer.created_at)}</p></div></CardContent></Card>)}</div>; }
